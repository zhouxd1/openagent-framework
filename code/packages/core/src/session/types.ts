/**
 * Session Types for OpenAgent Framework
 * 
 * Re-exports from interface to avoid duplication
 */

export {
  SessionConfig,
  Session,
  Message,
  PaginationOptions,
  PaginatedResult,
  SessionManagerConfig,
  SessionStats,
} from './interface';
